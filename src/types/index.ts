export interface MousePosition {
  x: number;
  y: number;
}

export interface NavItem {
  label: string;
  href: string;
}

export interface RaceData {
  name: string;
  track: string;
  date: string;
  image: string;
}
